package org.crypthing.things;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeQuarkusServiceIT extends QuarkusServiceTest {

    // Execute the same tests but in native mode.
}